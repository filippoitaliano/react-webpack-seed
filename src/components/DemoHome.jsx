import React from 'react'

const DemoComponent = () => (
  <p className="demo-component">
    Minimal 2020 React + Webpack seed project with client-side routing.
  </p>
);

export default DemoComponent;
